# Azure OpenAI Configuration (Required)
AZURE_OPENAI_ENDPOINT="https://your-resource.openai.azure.com/"
AZURE_OPENAI_DEPLOYMENT_NAME="gpt-5-mini"

# Azure AI Search (Optional - required only if you enable catalog search)
AZURE_SEARCH_ENDPOINT=
AZURE_SEARCH_INDEX_NAME=
# If omitted, backend will try DefaultAzureCredential (RBAC). For local dev, API key is easiest.
AZURE_SEARCH_API_KEY=

# Entra ID Authentication (Optional - leave empty to disable auth)
# Run ./setup-entra-auth.sh from project root to generate these values

# Frontend Configuration
VITE_ENTRA_CLIENT_ID=
VITE_ENTRA_TENANT_ID=
VITE_ENTRA_REDIRECT_URI=http://localhost:5173
VITE_ENTRA_API_SCOPE=

# Backend API Configuration
ENTRA_CLIENT_ID=
ENTRA_TENANT_ID=
ENTRA_AUDIENCE=

# Python Client Configuration (for client_raw.py)
ENTRA_PYTHON_CLIENT_ID=
ENTRA_API_SCOPE=
